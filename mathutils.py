def square(x: int) -> int:
    """Calculate the square of an integer.

    This function returns the product of ``x`` multiplied by itself,
    effectively computing ``x ** 2``.

    Args:
        x (int): The integer to be squared.

    Returns:
        int: The squared value.
    """
    return x * x
